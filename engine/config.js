const CONFIG = {
    appName: "Omniscient RP",
    version: "0.1.0",

    canonLock: true,
    autoSearch: true,
    autoMemory: true,

    providers: [
        "claude",
        "openai",
        "gemini",
        "openrouter"
    ],

    maxMemoryResults: 25,

    searchOnUnknownEntity: true
};
